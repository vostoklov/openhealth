# Dependencies
node_modules/

# Build output
dist/

# Environment variables (NEVER commit these)
.env
.env.local
.env.*.local

# deep-research skill: API keys + filled personal context stay local-only
.research_db_keys.json
*api*key*.json
*secret*.json
*.token
.claude/commands/research/context.md

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Health data (NEVER commit real health data)
*.health-data
*.health-export
data/personal/

# Local workspace created by `openhealth init` / setup.sh (real personal data)
/data/
/contexts/timeline/
/contexts/interventions/

# Dashboard real-data bridge output (real personal health data — local only)
ui/web/data.local.json

# SQLite databases
*.db
*.sqlite
*.sqlite3

# Logs
*.log
npm-debug.log*

# Coverage
coverage/

# TypeScript cache
*.tsbuildinfo

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.ruff_cache/
ui/web/refs/

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Атмосферные фоны: локальные (cosmos-артворки, не для распространения)
ui/web/assets/bg/

# Личный медиа-воркспейс для слайдов (фото из Photos, скрины WHOOP, заметки
# к нарезке) — локально, НЕ для публичного репо.
ui/web/assets/slides/

# Runtime intake envelopes (personal data) — written by POST /api/intake when the
# bridge serves ui/web. Never commit intake content to the public repo.
ui/web/data/
data/intake/
